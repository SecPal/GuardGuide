# SPDX-FileCopyrightText: 2026 SecPal
# SPDX-License-Identifier: CC0-1.0

name: Project Board Automation

on:
  issues:
    types: [opened, reopened, closed, labeled]
  pull_request:
    types: [opened, ready_for_review, closed, converted_to_draft, labeled]
  pull_request_review:
    types: [submitted]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  detect-project-automation-secrets:
    name: Detect project automation secrets
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      enabled: ${{ steps.detect.outputs.enabled }}
    steps:
      - name: Detect GitHub App credentials
        id: detect
        env:
          APP_ID: ${{ secrets.APP_ID }}
          APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
        run: |
          if [[ -n "$APP_ID" && -n "$APP_PRIVATE_KEY" ]]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

  project-automation:
    name: Project automation
    needs: detect-project-automation-secrets
    if: needs.detect-project-automation-secrets.outputs.enabled == 'true'
    uses: SecPal/.github/.github/workflows/project-automation-core.yml@2cc65df6215af0c4238d2d463869cfebfdc246ba
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}

  draft-reminder:
    name: Draft PR Reminder
    if: |
      github.event.pull_request != null &&
      github.event.action == 'opened' &&
      github.event.pull_request.draft == false
    uses: SecPal/.github/.github/workflows/draft-pr-reminder.yml@6cb41a892d9a26af1461c2e5daf150e70775f74a
    permissions:
      issues: write
      pull-requests: write
